# Abril 2026 — Tareas completadas

## 2026-04-10: Reorganización de docs y bootstrap del sistema pm-tasks

### DOCS-INIT: Inicializar sistema de task tracking

- [x] Crear `docs/TASK_TODO.md` como fuente única de tareas pendientes, con convención `added: YYYY-MM-DD` por tarea y priorización por sprint / siguiente / backlog
- [x] Crear `docs/TASK_COMPLETED/README.md` con las reglas del archivo mensual (formato `YYMM.md`, sin bloques de código, referencias a git/archivos, relación con `CHANGELOG.md`)
- [x] Crear `docs/TASK_COMPLETED/2604.md` (este archivo) — primera entrada del historial mensual
- [x] Crear `docs/archived/` como directorio separado para docs históricas no-tarea (post-mortems, parches de referencia, decisiones)
- [x] Establecer zonas protegidas: `README.md` y `CHANGELOG.md` del root + READMEs de módulos (`convex/`, `src/components/activities/`, etc.) no deben contener listas de tareas ni checklists. Solo documentación.

Notas: convención adoptada siguiendo la skill `pm-tasks`. `CHANGELOG.md` sigue siendo la fuente de historial de releases orientada a lectores externos; `TASK_COMPLETED/YYMM.md` es el log granular de trabajo interno. Ambas coexisten con propósitos distintos.

### DOCS-CLEANUP: Limpiar contaminación de task tracking en READMEs

- [x] Extraer la sección "Checklist de Implementación" de `src/components/activities/shared/core/README.md` al backlog central. Los 3 items completados (`TurnManager`, `PlayerManager`, tipos base) ya están implementados y documentados en el body del README; los 5 pendientes se movieron a `TASK_TODO.md` bajo la task `ACTIVITIES-CORE`.
- [x] Reemplazar la sección removida por una línea de referencia al backlog central para que cualquier lector futuro encuentre el estado actualizado en un solo lugar.

### DOCS-ARCHIVE: Archivar post-mortems históricos

- [x] Mover `docs/ds-followup/` → `docs/archived/2604-ds-followup/`. Esta carpeta contenía los 12 parches de las migraciones de design system diferidas del PR #1, aplicadas todas en PR #2 (merge `5b16e9b`). Ya no es trabajo pendiente, es referencia histórica del proceso de resolución.
- [x] Agregar un banner "✅ Status: applied" al tope del README archivado para que cualquier lector entienda inmediatamente que es post-mortem, no pendiente.

Notas: se usó `git mv` para preservar la historia de cada archivo individual. El naming `2604-ds-followup/` replica la convención mensual de `TASK_COMPLETED/` para que futuros archivos archivados se ordenen cronológicamente.

### DOCS-BACKLOG: Sembrar TASK_TODO con lo conocido pendiente

- [x] Agregar `ACTIVITIES-CORE` (5 piezas del core compartido de juegos por turnos) extraído del README de activities/shared/core
- [x] Agregar `DOCS-NITS` con los 2 ajustes menores identificados en memoria de sesión: subsección Phase 3.1 en `CHANGELOG.md` y mención del split de `HighCardGame` en el README de activities

Notas: estos items estaban flotando en la memoria de sesión (`project_kovan.md` en `~/.claude/projects/.../memory/`). Moverlos al backlog central los hace visibles para cualquier contributor, no solo para sesiones de Claude con memoria cargada.

---

> **Referencia cruzada**: El historial completo de Phase 3 (household gamification + design system + refactors estructurales + testing + CI) está documentado en [`../../CHANGELOG.md`](../../CHANGELOG.md) bajo la sección `[Phase 3]`. Los PRs mergeados fueron #1 (merge `153961e`), #2 (merge `5b16e9b`) y #3 (merge `4f6fa44`).
