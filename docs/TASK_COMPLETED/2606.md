# Junio 2026 — Tareas completadas

## 2026-06-10: MCP Tools Lote A — lecturas para simetría del catálogo

### MCP-TOOLS-A: Lecturas faltantes para simetría del catálogo MCP `added: 2026-06-10`

Catálogo pasa de 27 a 35 tools. Implementadas en `convex/lib/agent/`
(branch `feat/mcp-tools-lote-a`):

- [x] `searchContacts` + `addContact` — directorio con fuzzy match por nombre/especialidad, filtro por categoría y detección de duplicados (`tools.contacts.ts`)
- [x] `getPlaces` (filtros por categoría y visitado/pendiente) + `registerPlaceVisit` (fuzzy match, rating 1-5, fecha local) — lugares deja de ser write-only (`tools.places.ts`)
- [x] `listRecipes` con búsqueda fuzzy por título y filtro por categoría — recetas deja de ser write-only (`tools.recipes.ts`)
- [x] `getExpiringDocuments` — solo metadata (título, tipo, vencimiento, persona); nunca contenido ni números de documento (`tools.documents.ts`)
- [x] `getVehicleReminders` — próximos mantenimientos vía `nextDate` de `vehicleEvents`, ordenados y con marca de vencidos (`tools.vehicles.ts`)
- [x] `listFamilyMembers` — miembros activos con rol, marca quién es el usuario actual (`tools.family.ts`)

System prompt del agente actualizado con los dominios nuevos y la regla
de desambiguar con `listFamilyMembers`.
